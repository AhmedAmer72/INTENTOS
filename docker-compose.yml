services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: intentos
      POSTGRES_PASSWORD: intentos
      POSTGRES_DB: intentos
    ports:
      - "5432:5432"
    volumes:
      - intentos_pg:/var/lib/postgresql/data

volumes:
  intentos_pg:
