# Operational wallets (public addresses only)

Private keys live in gitignored `.wallets.json`. Regenerate with `pnpm provision` if these are unused.

| Role | Address |
| --- | --- |
| DEPLOYER | `0x66d0251da78C3905E25ABa98c0198DFaD1BDC7CF` |
| VERIFIER_ORACLE | `0xee066a65D5C3878254E4027D4fff76a9Ac427c74` |

Fund on Galileo (`16602`) from https://faucet.0g.ai then check:

```bash
pnpm balances
```
